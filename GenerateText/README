Compile project:
    javac -d bin/ src/*.java

Run project:
    java --class-path bin Application
    java --class-path bin Application 4

Package project:
    jar cfm dist/Application.jar res/Manifest.txt -C bin .

Run package:
    java -jar dist/Application.jar
    java -jar dist/Application.jar 4
    
You can change the data of the chain in the "GenerateTextFile.txt" file.